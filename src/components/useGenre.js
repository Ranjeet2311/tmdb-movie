// const useGenre = (setGenere) => {
//   //   if (setGenere.length < 1) return "";

//   const GenreIds = setGenere && setGenere.map((g) => g.id);
//   return GenreIds.reduce((accumulator, current) => accumulator + "," + current);
// };

// export default useGenre;
